name: imageclefmed-caption-2026
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - pip
  - numpy
  - pandas
  - pillow
  - tqdm
  - scikit-learn
  - jupyter
  - ipykernel
  - pip:
      - torch>=2.1
      - torchvision>=0.16
      - transformers>=4.41
      - accelerate>=0.30
      - evaluate>=0.4
      - rouge_score>=0.1.2
      - bert_score>=0.3.13
      # Optional:
      # - bleurt
      # - medcat
      # - alignscore
